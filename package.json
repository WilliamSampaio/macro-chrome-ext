{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "macrotap",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension for saving page elements as macro steps and replaying them.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/background.js && node --check src/content.js && node --check popup/popup.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest ok')\""
  }
}
